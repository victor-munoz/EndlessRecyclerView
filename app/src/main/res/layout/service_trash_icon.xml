<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/little_circle_image"
    android:layout_height="@dimen/little_circle_image"
    android:gravity="center"
    android:id="@+id/chathead_relativelayout">

    <ImageView
        android:layout_width="@dimen/icon_trash"
        android:layout_height="@dimen/icon_trash"
        android:id="@+id/trash_icon"
        android:scaleType="center"
        android:src="@drawable/icon_close"
        android:background="@drawable/close_background"
        android:contentDescription="@string/trash_icon" />



</RelativeLayout>